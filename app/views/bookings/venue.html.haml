%h1 Venue Booking

%ul
  - @rooms.each do |room|
    %li
      = room.name 
      =  link_to 'Book',"/bookings/new?bookable_id=#{room.id}&bookable_type=Venue"
%ul
  - @timeslots.each do |slot|
    %li= slot

= link_to 'Continue', booking_confirmation_path, class: 'btn'