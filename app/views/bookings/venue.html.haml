%h1 Venue Booking

%ul
  - @rooms.each do |room|
    %li= room.name #change to room.name to display the venue name

%ul
  - @timeslots.each do |slot|
    %li= slot

= link_to 'Continue', booking_confirmation_path, class: 'btn'